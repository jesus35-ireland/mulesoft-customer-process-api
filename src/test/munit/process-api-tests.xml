<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:munit="http://www.mulesoft.org/schema/mule/munit"
      xmlns:munit-tools="http://www.mulesoft.org/schema/mule/munit-tools"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/munit http://www.mulesoft.org/schema/mule/munit/current/mule-munit.xsd
        http://www.mulesoft.org/schema/mule/munit-tools http://www.mulesoft.org/schema/mule/munit-tools/current/mule-munit-tools.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

    <!-- ========================= -->
    <!-- MOCK SYSTEM API (GLOBAL) -->
    <!-- ========================= -->
    <munit:before-test>
        <munit-tools:mock-when processor="http:request">
            <munit-tools:then-return>
                <munit-tools:payload value="#[{}]"/>
            </munit-tools:then-return>
        </munit-tools:mock-when>
    </munit:before-test>

    <!-- ===================================================== -->
    <!-- GET ALL CUSTOMERS -->
    <!-- ===================================================== -->
    <munit:test name="get-all-customers">
        <munit:behavior>
            <munit-tools:mock-when processor="http:request">
                <munit-tools:then-return>
                    <munit-tools:payload value='#[{
                        success: true,
                        data: [{id: 1, name: "John"}]
                    }]'/>
                </munit-tools:then-return>
            </munit-tools:mock-when>
        </munit:behavior>

        <munit:execution>
            <set-variable variableName="correlationId" value="test"/>
            <flow-ref name="get:\customers:customer-process-api-config"/>
        </munit:execution>

        <munit:validation>
            <munit-tools:assert-that expression="#[payload.success]"
                                     is="#[MunitTools::equalTo(true)]"/>
        </munit:validation>
    </munit:test>

    <!-- ===================================================== -->
    <!-- GET CUSTOMER BY ID -->
    <!-- ===================================================== -->
    <munit:test name="get-customer-by-id">
        <munit:execution>
            <set-variable variableName="id" value="1"/>
            <flow-ref name="get:\customers\(id):customer-process-api-config"/>
        </munit:execution>
    </munit:test>

    <!-- ===================================================== -->
    <!-- POST CUSTOMER -->
    <!-- ===================================================== -->
    <munit:test name="post-customer">
        <munit:behavior>
            <munit-tools:mock-when processor="http:request">
                <munit-tools:then-return>
                    <munit-tools:payload value="#[{id: 100}]"/>
                </munit-tools:then-return>
            </munit-tools:mock-when>
        </munit:behavior>

        <munit:execution>
            <set-variable variableName="correlationId" value="test"/>
            <set-payload value="#[{name:'John'}]"/>
            <flow-ref name="post:\customers:application\json:customer-process-api-config"/>
        </munit:execution>

        <munit:validation>
            <munit-tools:assert-that expression="#[payload.customerId]"
                                     is="#[MunitTools::equalTo(100)]"/>
            <munit-tools:assert-that expression="#[payload.status]"
                                     is="#[MunitTools::equalTo('CREATED')]"/>
        </munit:validation>
    </munit:test>

    <!-- ===================================================== -->
    <!-- PUT CUSTOMER -->
    <!-- ===================================================== -->
    <munit:test name="put-customer">
        <munit:behavior>
            <munit-tools:mock-when processor="http:request">
                <munit-tools:then-return>
                    <munit-tools:payload value='#[{
                        id: 10,
                        rowsAffected: { affectedRows: 1 }
                    }]'/>
                </munit-tools:then-return>
            </munit-tools:mock-when>
        </munit:behavior>

        <munit:execution>
            <set-variable variableName="id" value="10"/>
            <set-variable variableName="correlationId" value="test"/>
            <set-payload value="#[{name:'update'}]"/>
            <flow-ref name="put:\customers\(id):application\json:customer-process-api-config"/>
        </munit:execution>

        <munit:validation>
            <munit-tools:assert-that expression="#[payload.status]"
                                     is="#[MunitTools::equalTo('UPDATED')]"/>
        </munit:validation>
    </munit:test>

    <!-- ===================================================== -->
    <!-- DELETE CUSTOMER OK -->
    <!-- ===================================================== -->
    <munit:test name="delete-customer-ok">
        <munit:behavior>
            <munit-tools:mock-when processor="http:request">
                <munit-tools:then-return/>
            </munit-tools:mock-when>
        </munit:behavior>

        <munit:execution>
            <set-variable variableName="id" value="1"/>
            <set-variable variableName="correlationId" value="test"/>
            <flow-ref name="delete:\customers\(id):customer-process-api-config"/>
        </munit:execution>

        <munit:validation>
            <munit-tools:assert-that expression="#[payload.status]"
                                     is="#[MunitTools::equalTo('DELETED')]"/>
        </munit:validation>
    </munit:test>

    <!-- ===================================================== -->
    <!-- DELETE CUSTOMER NOT FOUND -->
    <!-- ===================================================== -->
    <munit:test name="delete-customer-not-found">
        <munit:behavior>
            <munit-tools:mock-when processor="http:request">
                <munit-tools:then-throw errorType="HTTP:NOT_FOUND"/>
            </munit-tools:mock-when>
        </munit:behavior>

        <munit:execution>
            <set-variable variableName="id" value="99"/>
            <set-variable variableName="correlationId" value="test"/>
            <flow-ref name="delete:\customers\(id):customer-process-api-config"/>
        </munit:execution>

        <munit:validation>
            <munit-tools:assert-that expression="#[payload.status]"
                                     is="#[MunitTools::equalTo('NOT_FOUND')]"/>
        </munit:validation>
    </munit:test>

    <!-- ===================================================== -->
    <!-- MAIN FLOW ERROR BAD_REQUEST -->
    <!-- ===================================================== -->
    <munit:test name="main-bad-request">
        <munit:execution>
            <munit-tools:raise-error type="APIKIT:BAD_REQUEST"/>
        </munit:execution>

        <munit:validation>
            <munit-tools:assert-that expression="#[payload.message]"
                                     is="#[MunitTools::equalTo('Bad request')]"/>
        </munit:validation>
    </munit:test>

    <!-- ===================================================== -->
    <!-- MAIN FLOW NOT FOUND -->
    <!-- ===================================================== -->
    <munit:test name="main-not-found">
        <munit:execution>
            <munit-tools:raise-error type="APIKIT:NOT_FOUND"/>
        </munit:execution>

        <munit:validation>
            <munit-tools:assert-that expression="#[payload.message]"
                                     is="#[MunitTools::equalTo('Resource not found')]"/>
        </munit:validation>
    </munit:test>

</mule>